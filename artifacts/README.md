# Generated artifacts

- `checkpoints/`: model weights and checkpoint metadata.
- `embeddings/`: NumPy/Parquet embedding matrices and reaction-ID tables.
- `reports/`: training histories, diagnostics, plots, and validation reports.

These files are reproducible outputs and are excluded from ordinary Git commits. Use release assets, object storage, or Git LFS when a frozen artifact set must be published.
